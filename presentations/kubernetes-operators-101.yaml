apiVersion: presentations.haavard.dev/v1alpha1
kind: Presentation
metadata:
  name: kubernetes-operators-101
spec:
  theme:
    primaryColor: "#FFFFFF"
    secondaryColor: "#3366FF"
    backgroundColor: "#000000"
    fontFamily: "'Neue Haas Grotesk Pro', 'Neue Haas Grotesk', sans-serif"
    logo: "https://i.imgur.com/f9KKoJq.png"
  slides:
    - title: "Stop clicking. Start declaring."
    - title: ""
      images:
        - url: "https://github.com/Haavasma/operator-sdk-demo-v2/releases/download/videos-v2/problem-toil.gif"
          alt: "Months of tickets for one endpoint"
      bullets:
        - "Firewall team, security team, ingress team — months of calls and tickets"
        - "The code took an afternoon. The ops took a quarter."
        - "Knowledge trapped in notion, confluence, Slack threads, people's heads"
        - "Next team that needs this? Starts from zero."
        - "How do we encode operations as code?"
    - title: "Step 1: Kubernetes"
      images:
        - url: "https://github.com/Haavasma/operator-sdk-demo-v2/releases/download/videos-v2/k8s-reconcile.gif"
          alt: "Kubernetes reconciles desired state: pod dies, controller replaces it"
      bullets:
        - "Declarative orchestrator for containerized workloads"
        - "Desired state in YAML, controllers reconcile toward it"
        - "Built-in primitives: Deployment, Service, Ingress, ConfigMap..."
        - "Self-healing control loops handle drift and failure"
    - title: "Step 2: GitOps"
      images:
        - url: "https://github.com/Haavasma/operator-sdk-demo-v2/releases/download/videos-v2/gitops-sync.gif"
          alt: "Git commit flows through GitOps agent into cluster"
      bullets:
        - "Git as single source of truth for cluster state"
        - "Agent continuously syncs repo → cluster"
        - "Auditable, reviewable, reproducible deployments"
        - "But: only as expressive as the resources you apply"
        - "Complex apps still need glue scripts, Helm hacks, manual steps"
    - title: "Step 3: Custom Resources"
      bullets:
        - "Extend the Kubernetes API with your own resource types"
        - "Domain-specific abstractions: Database, Presentation, Pipeline"
        - "Users write high-level specs, not low-level plumbing"
        - "GitOps still works — now over *your* abstractions"
        - "Needs a controller to give the CRD meaning"
    - title: "Step 4: Operator SDK"
      bullets:
        - "Framework for building the controller behind a CRD"
        - "Encodes operational knowledge as a reconciliation loop"
        - "Turn runbooks into code that runs 24/7"
        - "Problem → solved: ops is now declarative, versioned, automated"
    - title: "Operator pattern"
      images:
        - url: "https://iximiuz.com/kubernetes-operator-pattern/kube-control-loop-3000-opt.png"
    - title: "CRD + CR: define the type, then instantiate"
      images:
        - url: "https://github.com/Haavasma/operator-sdk-demo-v2/releases/download/videos-v2/crd-abstraction.gif"
          alt: "Define a CRD for Greeting, then create a Greeting instance"
      bullets:
        - "CRD defines the schema (like a class)"
        - "CR is an instance of that type (like an object)"
        - "kubectl treats CRs like any built-in resource"
        - "A controller gives the CR meaning (next step)"
    - title: "When to reach for a CRD"
      bullets:
        - "Encode operational knowledge as a resource"
        - "Provide a native Kubernetes experience"
        - "Abstract away complex, repetitive setup"
        - "Manage external resources from inside the cluster"
        - "Use cases: databases, message queues, pipelines, tenants"
    - title: "Operator SDK"
      bullets:
        - "Framework for building operators — handles boilerplate"
        - "Options: Helm, Kustomize, or Go"
        - "Go flavor: define the CRD via Go types, write the reconcile loop"
        - "Auto-generates CRD manifests, RBAC, deepcopy code"
    - title: "Why Operator SDK?"
      bullets:
        - "Standardized way to build operators"
        - "Strong typing instead of untyped YAML"
        - "Kubernetes is written in Go — native ecosystem"
        - "Integrate any tooling or external system"
        - "Performant, DRY, and batteries-included"
    - title: "DEMO"
